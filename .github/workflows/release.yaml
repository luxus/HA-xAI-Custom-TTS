name: "Release"

on:
  release:
    types:
      - published
permissions:
  contents: write
  packages: write


jobs:
  release:
    name: Prepare release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Create zip file
        run: |
          cd custom_components/spacexai
          zip -r ../../spacexai.zip . -x "__pycache__/*" "*.pyc" "*.pyo"

      - name: Upload zip to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ./spacexai.zip
          asset_name: spacexai.zip
          tag: ${{ github.event.release.tag_name }}
          overwrite: true
