get_voices:
  name: Get Voices
  description: Retrieve built-in and custom xAI voices (GET /v1/tts/voices and GET /v1/custom-voices) with optional filtering
  fields:
    search_text:
      name: Search Text
      description: Search for voices by name, type, tone, or description
      required: false
      example: "female"
      selector:
        text:
